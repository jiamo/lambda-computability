# M7-LICENSE

**Status:** DONE_STRONG

The repository had no licence file, so it was not usable as open source.

* `LICENSE` at the repository root now contains the full text of the **Apache License,
  Version 2.0**, including the appendix boilerplate filled in for this project.
* `README.md` has a `License` section pointing at it.

Note: the Lean sources import Mathlib, which is itself distributed under Apache-2.0, so the
choice is compatible with the dependency.
