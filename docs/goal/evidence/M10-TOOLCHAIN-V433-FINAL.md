# M10-TOOLCHAIN-V433-FINAL

**Status:** DONE_STRONG

The repository builds on the toolchain it documents, Lean `v4.33.0`, with Mathlib and `cslib`
resolved as pinned *git* dependencies, with no errors and no warnings.  This supersedes
`M10-TOOLCHAIN-V428-REPIN`, which had moved the sources back to the older APIs of Lean `v4.28.0`.

## The pin

* `lean-toolchain`: `leanprover/lean4:v4.33.0`;
* Mathlib: `[[require]] name = "mathlib", scope = "leanprover-community", rev = "v4.33.0"`,
  resolved to commit `db584cd6d4…`;
* `cslib`: `[[require]] name = "cslib", git = "https://github.com/leanprover/cslib",
  rev = "3951377e5a3f5772737f11cd62bc5bb6a72f95d1"` — the revision that moves `cslib` to Lean
  `v4.33.0`;
* `lake-manifest.json` regenerated by `lake update`, so it records exactly those revisions.

Earlier pins used `path` requirements into `.lake/packages/`; those checkouts are no longer
referenced, and `README.md` now describes the git resolution that `lakefile.toml` performs.

## Sources brought forward to the v4.33 APIs

* `Start/TM2Forward.lean`, `Start/TM2Capstone.lean` — `Turing.Reaches`, `Turing.eval`,
  `Turing.mem_eval` are `StateTransition.Reaches`, `StateTransition.eval`,
  `StateTransition.mem_eval`.
* `Start/TM2Partrec.lean`, `Start/TM2PolyTime.lean` — `Computability.FinEncoding` is replaced by a
  bare encoding function `ea : α → List αΓ` together with a `Fintype` alphabet.
* `Start/CwaType.lean` — morphisms of `Type u` are the bundled `TypeCat.Hom`, so the standard
  model uses `↾f` and `TypeCat.homEquiv`.
* `Start/CwaTwoCellUniv.lean`, `Start/LambdaPiInterpTransport.lean` — rewriting chains need a
  closing `rfl` under the new `simp` normal forms.
* `Start/OracleSim.lean` — `RecursiveIn` on `ℕ` is now defined through encodings; the bridge is
  `RecursiveIn.iff_nat`, and `Part.mem_bind_iff` must be applied explicitly.
* `Start/GapTheorem.lean` — `push_neg` is `push Not`; `computable_goodB` needs a larger
  `maxHeartbeats` budget, which is recorded with an explanatory comment.
* `Start/OracleJump.lean`, `Start/Representation.lean` — `cslib` presents full β-reduction as the
  congruence closure `Xi Beta`, so the constructors are `Term.Xi.base (Term.Beta.beta …)`,
  `Term.Xi.appL`, `Term.Xi.appR`, `Term.Xi.abs`; `Term.subst_intro` no longer takes a local-closure
  argument; confluence is `Term.confluent_fullBeta`.
* Deprecation and linter follow-ups across the library: `Set.mem_setOf(_eq)` →
  `Set.mem_ofPred(_eq)`, `Mathlib.Data.Nat.Lattice` → `Mathlib.Order.Lattice.Nat`, removal of
  no-op `change`/`beta_reduce` tactics, `@[instance_reducible]` on the three instances the linter
  asks for, and `set_option linter.checkUnivs false in` before the docstrings of `Cwa.CModel`,
  `Cwa.Model` and `Cwa.PbCat`, exactly as Mathlib does for `CategoryTheory.Cat`.

## Gates

```
lake build                       # Build completed successfully (9051 jobs), 0 errors, 0 warnings
python3 scripts/check_closure.py # OK: 331 modules, all in the import closure and all registered
python3 scripts/goal_state.py validate
```

No module under `Start/` contains `sorry` or `admit`.
